// ignore_for_file: constant_identifier_names

enum AppTabs {
  shop,
  exchange,
  globe,
  launchpad,
  wallet,
}

enum CoinType {
  Cryptocurrency,
  NFT,
}

enum SortType {
  market_cap,
  price,
  percent_change_24h,
}

enum SortBy {
  desc,
  asc,
}
